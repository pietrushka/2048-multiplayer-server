{
  "name": "2048.vs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "pnpm test -r",
    "startServer": "pnpm --filter backend start",
    "zipRepoForEB": "rm -rf repo_boundle.zip && zip -r repo_boundle.zip ./ -x '*node_modules/*' -x '*.vscode/*' -x '*dist/*' -x '*__tests__/*' -x '*.git/*' -x 'apps/web/*'",
    "unzip": "rm -rf ./temp && unzip repo_boundle.zip -d ./temp",
    "docker": "pnpm prepare && pnpm zipRepoForEB && pnpm unzip && cd temp && docker compose up"
  },
  "license": "ISC",
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "^7.1.1",
    "eslint": "^8.56.0",
    "prettier": "^3.2.5",
    "tsup": "^8.0.1",
    "typescript": "^5.4.2"
  }
}